l1 = input().split(',')
l2 = input().split(',')

combine_nums = []

for index in range(len(l1)):
    combine_nums.append(l1[index])
    combine_nums.append(l2[index])
    index += 1

print(','.join(combine_nums))
